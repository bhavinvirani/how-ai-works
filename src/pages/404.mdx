---
layout: ../layouts/BaseLayout.astro
title: Page not found — How AI Actually Works
description: That page does not exist.
---

import { withBase } from '../lib/paths';

# Page not found

There is nothing at this address.

The site is still being built, so it is entirely possible the page you wanted
has not been written yet, rather than that you mistyped something.

<p>
  <a href={withBase('/')}>Go back to the start</a>
</p>
